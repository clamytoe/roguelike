from __future__ import annotations
from enum import Enum, auto


class EquipmentSlots(Enum):
    """Enumeration of valid equipment slots for equippable items."""

    MAIN_HAND = auto()
    OFF_HAND = auto()
    # HEAD = auto()
    # BODY = auto()
    # LEGS = auto()
    # FEET = auto()
    # RING = auto()
    # AMULET = auto()
